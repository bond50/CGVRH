import React from 'react';
import {APP_NAME} from "../../config";

const Population = () => {
    return (

        <p>
            {`${APP_NAME}'s`} catchment population extends beyond Vihiga County to neighboring counties like Kisumu,
            Kakamega,
            Siaya, and Nandi. The hospital is a critical healthcare provider in the region, adapting to changes brought
            about by healthcare reforms in Kenya.
        </p>

    );
};

export default Population;