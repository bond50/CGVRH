import Layout from "../../hoc/Layout";
import Partners from "../../components/about/Partners";

const Partner = () => {
    return (
        <Layout>
            <Partners/>
        </Layout>
    );
};

export default Partner;